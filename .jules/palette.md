## 2024-06-04 - Accessible Icon Buttons
**Learning:** Icon-only interactive elements (like in Cart/Wishlist) consistently lack `aria-label` attributes across the application, degrading the screen reader experience.
**Action:** Always verify and include descriptive `aria-label` attributes (e.g., `aria-label="Close cart"`) on all icon-only buttons to ensure full accessibility.
