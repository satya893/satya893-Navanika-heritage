## 2024-05-27 - Icon-only buttons lack ARIA labels
**Learning:** Found multiple icon-only buttons across components (`Cart`, `ProductDetails`, `ProductGrid`, etc.) missing `aria-label` attributes for accessibility.
**Action:** When adding or reviewing icon-only buttons, always add descriptive `aria-label`s.
